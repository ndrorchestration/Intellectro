# Supply-chain install policy.
# Existing package-lock.json remains the install source of truth; npm ci must not rewrite it.
min-release-age=7
allow-git=root
allow-remote=root
allow-file=root
allow-directory=root
